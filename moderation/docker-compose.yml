version: '3.9'

services:
  postgres:
    image: postgres:15
    container_name: neomarket_moderation_db
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: neomarket_moderation
    ports:
      - "5434:5432"
    volumes:
      - postgres_moderation_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5

  # ТЕСТОВАЯ БАЗА ДАННЫХ ДЛЯ ТЕСТОВ
  postgres_test:
    image: postgres:15
    container_name: neomarket_moderation_test_db
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: neomarket_moderation_test
    ports:
      - "5435:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5

  pgadmin:
    image: dpage/pgadmin4
    container_name: neomarket_pgadmin_moderation
    environment:
      PGADMIN_DEFAULT_EMAIL: admin@neomarket.ru
      PGADMIN_DEFAULT_PASSWORD: admin
    ports:
      - "5051:80"
    depends_on:
      - postgres

  moderation-service:
    build: .
    container_name: neomarket_moderation
    ports:
      - "8001:8000"
    environment:
      DB_HOST: postgres
      DB_PORT: 5432
      DB_NAME: neomarket_moderation
      DB_USER: postgres
      DB_PASS: postgres
      DATABASE_URL: postgresql+psycopg2://postgres:postgres@postgres:5432/neomarket_moderation
      B2B_SERVICE_URL: http://b2b-service:8000
      # Для тестов
      TEST_DATABASE_URL: postgresql+psycopg2://postgres:postgres@postgres_test:5432/neomarket_moderation_test
    volumes:
      - .:/app
      - ./migrations:/app/migrations
    command: bash -c "alembic upgrade head && uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload"
    depends_on:
      postgres:
        condition: service_healthy

volumes:
  postgres_moderation_data: